apiVersion: v1
kind: Secret
metadata:
  name: agent-framework-secret
  namespace: ai-agents
type: Opaque
data:
  # IMPORTANT: Replace these base64-encoded values with your actual secrets
  # To encode: echo -n "your-secret-here" | base64
  
  # OpenAI API Key (REQUIRED)
  # Example: echo -n "sk-your-key-here" | base64
  OPENAI_API_KEY: c2stWU9VUi1PUEVOQUktQVBJLUtFWS1IRVJF
  
  # API Key for authentication (OPTIONAL)
  # Example: echo -n "your-api-key" | base64
  API_KEY: eW91ci1hcGkta2V5LWhlcmU=
  
  # Database Password (if using external PostgreSQL)
  # Example: echo -n "your-db-password" | base64
  DB_PASSWORD: YWdlbnRfcGFzc3dvcmQ=
